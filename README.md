This repository contains TalTech Databases course project files. In this project a lab database was created using SQL. In the folder Database\_files are the Entity Relationship Diagram (ERD) of the database (lab\_database\_ERD\_design.qsee), the SQL script for generating the database (lab\_database\_creation.sql) and script for inserting and querying data (database\_queries.sql). 

